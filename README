utils
=====

Vim settings and setup, minimal templates, reset styles, and some more. Practical pieces of code and text.

Up to this moment, the following parts are included:

- A sample vimrc configuration file (vimrc);
- A text file with helpful vim commands (vim.txt);
- A vim install and setup Bash script (vimsetup.sh);
- An HTML5 markup template (template.html);
- A CSS reset file (reset.css);
- A JavaScript source code minification program (mini.c).
